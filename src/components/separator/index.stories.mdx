import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

import Separator from ".";

<Meta title="Components/Separator" component={Separator} />

# Separator

The `<Separator />` component is used to break up content. The `content` property can be set via `separatorString` prop.

The component is using a `<span>` that can be customized as a whole. The `content` property renders the `separatorString` prop, in this way. The default separator is set in Theme Settings.

## Usage

```jsx
import { Separator } from "@wpmedia/arc-themes-components";

const Feature = () => (
	<p>
		Everything needed: news <Separator /> Features <Separator separatorString="🚨" /> Breaking news.
	</p>
);
```

## Properties

<ArgsTable of={Separator} />

## Stories

** Default **

<Canvas>
	<Story name="Default">
		<p>
			My name
			<Separator />
			Your name here
		</p>
	</Story>
</Canvas>

** Custom string **

<Canvas>
	<Story name="Custom string">
		<p>
			<Separator separatorString="||" /> this <Separator separatorString="||" /> or{" "}
			<Separator separatorString="||" /> that <Separator separatorString="||" />
		</p>
	</Story>
</Canvas>

** Custom string with emoji **

<Canvas>
	<Story name="Custom string with emoji">
		<p>
			<Separator separatorString="🚨" />
			alert
			<Separator separatorString="🚨" /> alert
			<Separator separatorString="🚨" /> alert
			<Separator separatorString="🚨" />
		</p>
	</Story>
</Canvas>
